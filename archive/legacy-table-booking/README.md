# Legacy table-booking copies

These files are obsolete duplicate versions that were previously left in the active source tree with ` 2` in their filenames.

They are retained only for historical reference. The `.txt` suffix prevents Next.js and TypeScript from treating them as application source.
